export type ImagePlaceholder = {
  id: string;
  description: string;
  imageUrl: string;
  imageHint: string;
};

// This file is now empty as we are fetching images from the Data Dragon API.
export const PlaceHolderImages: ImagePlaceholder[] = [];
